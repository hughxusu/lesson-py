# 序列类型

Python 中内置了一些可同时存储多个数据的类型，这些类型统称为序列类型。

* 序列提供了一系列特有的操作方法。
* 序列类型分为可变序列和不可变序列。
* 序列类型是 Python 内置的数据类型和传统意义上的数据结构，堆、栈、链表等不同。

```mermaid
graph 
    a(序列类型)-->b(可变序列)
    a-->c(不可变序列)
    b-->e(list列表)
    b-->f(dict字典)
    b-->g(set集合)
    c-->h(string)
    c-->j(tuple元组)
```

